<!--
    If you want to enable Blazor routing, you should remove all of this contents,
    and uncomment the below line. (See also: https://docs.microsoft.com/aspnet/core/blazor/routing )

<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
-->

<div style="text-align: center;">
    <IgbChip Size="SizableComponentSize.Large" Selectable="false">Welcome to Ignite UI for Blazor</IgbChip>
</div>

@code {

}